#include "render_graph_vk.hh"

Result<void> RenderGraph::init(GPUAdapter& gpu) {
    return Ok();
}

Result<void> RenderGraph::dispatch(GPUAdapter &gpu) {
    return Ok();
}

Result<void> RenderGraph::destroy(GPUAdapter& gpu) {
    return Ok();
}
